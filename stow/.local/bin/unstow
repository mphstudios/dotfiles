#!/usr/bin/env sh
#
# Unlink packages managed using GNU Stow,
# shorthand for stow remove (stow --delete).
#
# Usage:
#   unstow <package>...             confirmation before removal
#   unstow -f|--force <package>...  remove without confirmation
#
exec stow remove "$@"
