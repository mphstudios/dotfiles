# cURL configuration options
# http://curl.haxx.se/docs/manpage.html

# Limit the time allowed in seconds to establish a connection before timeout
connect-timeout = 60

# Follow HTTP redirects
location

# Display progress as a simple progress bar
progress-bar

# Show error messages
show-error

# Send a constant user agent string
user-agent = "Mozilla/5.0 Gecko"
