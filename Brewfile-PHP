##
# Homebrew formulae for PHP
# https://github.com/Homebrew/homebrew-php
#
# Running `brew bundle` with this file will invoke each line as a brew command.
#
##

## Make sure we're using the latest Homebrew
update

## Upgrade any all-ready installed formulae
upgrade

## Add dependency repositories
tap homebrew/dupes
tap homebrew/versions

## Use Homebrew PHP repository
tap homebrew/php

install php55 --with-apache --with-homebrew-curl --with-homebrew-openssl --with-pgsql
install php55-imagick
install php55-intl
install php55-mongo
install php55-tidy
install php55-twig
install php55-xdebug
install phpunit
install composer

## Database web interfaces
install phpmyadmin
install phppgadmin

## Remove outdated versions from the Cellar
cleanup
