# Get editor completions based on the config schema
"$schema" = 'https://starship.rs/config-schema.json'

# Inserts a blank line between shell prompts
add_newline = true

command_timeout = 3000

format = """
$username\
$hostname\
${custom.dirs}\
$directory\
$git_branch\
$git_commit\
$git_state\
$git_status\
$golang\
$nodejs\
$python\
$ruby\
$rust\
$mise\
$sudo\
$jobs\
$line_break\
$container\
$kubernetes\
$shell\
$character
"""

right_format = '$cmd_duration $status'

continuation_prompt = '[↪](white) '

palette = 'gradoo'

[palettes.gradoo]
# TODO

[character]
error_symbol = '[](white)' # nf-cod-error
success_symbol = '[❯](white)'
vimcmd_symbol = '[❮](bold green)'
vimcmd_replace_symbol = '[❮](bold purple)'
vimcmd_visual_symbol = '[❮](bold yellow)'

[cmd_duration]
format = '[$duration]($style)'
style = 'bold yellow'

[custom.dirs]
# Show current position in the dirs stack when there is more than one entry.
# Nushell std/dirs module exports DIRS_LIST (a colon-separated list of paths)
# and DIRS_POSITION (zero-based) directly; explicitly use `sh` to expand these
# variables. Adds 1 to convert zero-based dirs index to display "index(depth)"
command = '''
colons=$(printf '%s' "$DIRS_LIST" | tr -cd ':' | wc -c | tr -d ' ')
depth=$((colons + 1))
printf "%d(%d)" "$((DIRS_POSITION + 1))" "$depth"
'''
when = 'printf "%s" "$DIRS_LIST" | grep -q ":"'
shell = ['sh', '-c'] # Nushell does not expand "$VAR" (explicitly use shell)
use_stdin = false
format = '[$symbol $output]($style) '
style = 'cyan'
symbol = '' # nf-oct-stack

[directory]
format = '[$path]($style)[$read_only]($read_only_style) '
style = 'bold cyan'
home_symbol = '♥' # nf-oct-heart
read_only = ' 󰩏' # nf-md-hand_front_right
read_only_style = 'bold red'
repo_root_format = '[$before_root_path]($before_repo_root_style)[$repo_root]($repo_root_style)[$path]($style)[$read_only]($read_only_style) '
repo_root_style = 'underline white'
truncation_length = 7
truncation_symbol = '…/'

[docker_context]
disabled = true

[git_branch]
style = 'bold magenta'
symbol = '' # nf-oct-git_branch
format = 'on [$branch(:$remote_branch)]($style) '
truncation_symbol = '…'

[git_commit]
commit_hash_length = 7
format = '[ $hash$tag]($style) ' #nf-oct-git_commit
tag_disabled = false
tag_symbol = '  ' # nf-oct-tag

[git_state]
bisect = 'BISECTING'
cherry_pick = '[🍒 PICKING](bold red)'
merge = ' MERGING' # nf-oct-git_merge
rebase = ' REBASING' # nf-oct-git_pull_request
revert = ' REVERTING' # nf-oct-git_merge_queue
format = '[\($state( $progress_current of $progress_total)\)]($style)'

[git_status]
format = '([$all_status( $ahead_behind)]($style)) '
ahead = '🢁${count}' # nf-cod-git_commit
behind = '🢃${count}' # nf-cod-git_commit
conflicted = '' # nf-fa-not_equal
deleted = '❕'
diverged = '🢁🢃' # nf-cod-git_commit
modified = '🗯️'
renamed = '' # nf-oct-diff_renamed
staged = '[+$count](green)'
stashed = ' ' # nf-cod-bookmark
style = 'white'
untracked = '❔'
up_to_date = '' # nf-cod-pass

[golang]
format = '[$symbol($version )]($style) '
symbol = 'Go'
version_format = '${raw}'

[hostname]
disabled = false
format = '[$ssh_symbol](bold blue)[$hostname]($style) '
ssh_only = false
ssh_symbol = 'SSH:'
style = 'yellow'

# The job module always shows one job if the command is not empty,
# caused by running atuin, which calls job spawn for each command;
# resulting in $number always being off by one when running atuin.
# See starship#7098 starship#7102 ...
[jobs]
format = '[$symbol$number]($style) '
style = 'underline blue'
symbol = ''

[kubernetes]
disabled = true

[mise]
disabled = false
format = '[$symbol $health]($style) '
healthy_symbol = '󱐋' # nf-md-lightning_bolt
style = 'bold purple'
symbol = 'mise'
unhealthy_symbol = '' # nf-oct-alert

[nodejs]
format = '[$symbol $version]($style) '
symbol = 'Node'
version_format = '${raw}'

[python]
format = '[$symbol $version]($style) '
pyenv_version_name = true
symbol = 'Python'
version_format = '${raw}'

[ruby]
format = '[$symbol $version]($style) '
symbol = 'Ruby'
version_format = '${raw}'

[rust]
format = '[$symbol $version]($style) '
symbol = 'Rust'
version_format = '${raw}'

[shell]
disabled = false
format = '[$indicator]($style)'
style = 'white'
bash_indicator = '' # nf-seti-shell
fish_indicator = '🐟'
nu_indicator = '' # default indicator
unknown_indicator = '' # nf-fa-question
zsh_indicator = '' # nf-fa-percent

[sudo]
disabled = false
format = '[as $symbol]($style) '
style = 'bold green'
symbol = '' # nf-dev-apple

[username]
disabled = true
format = '[$user]($style)'
show_always = true
style_root = 'red'
style_user = 'white'
