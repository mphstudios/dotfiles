#
# Path configuration
# OSX default path is defined in /private/etc/paths
#

# Ensures user-installed binaries take precedence.
PATH=/usr/local/bin:/usr/local/sbin:/usr/bin:/bin:/usr/sbin:/sbin

# Python version management is added to path by ~/.pyenvrc

# Use local gem executables and bundler binstubs when invoked the project root directory.
PATH=./bin:./.bundle/bin:$PATH

# Ruby version management
PATH=$HOME/.rvm/bin:$PATH

# Use local node modules when invoked from the project root directory.
PATH=./node_modules/.bin:$PATH

# Node version managment shims
PATH=$HOME/.nodenv/bin:$PATH

# Yarn JavaScript package manager
PATH=$PATH:`yarn global bin`

# Go language binaries
PATH=$PATH:/usr/local/opt/go/libexec/bin

# Haskell binaries
PATH=$PATH:$HOME/Library/Haskell/bin

# Java binaries
PATH=$PATH:$JAVA_HOME/bin

# Android SDK
PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_PLATFORM_TOOLS

export PATH
